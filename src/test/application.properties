# Test configuration
%test.quarkus.http.port=8080
%test.quarkus.datasource.db-kind=postgresql
%test.quarkus.datasource.username=${QUARKUS_DATASOURCE_USERNAME}
%test.quarkus.datasource.password=${QUARKUS_DATASOURCE_PASSWORD}
%test.quarkus.datasource.jdbc.url=${QUARKUS_DATASOURCE_JDBC_URL}

%test.quarkus.hibernate-orm.database.generation.database-kind=none
%test.quarkus.hibernate-orm.log.sql=true

%test.quarkus.flyway.migrate-at-start=true
%test.quarkus.flyway.locations=db/migration

# JWT configuration for tests
%test.quarkus.smallrye-jwt.enabled=true

%test.smallrye.jwt.sign.key.location=${SMALLRYE_JWT_SIGN_KEY_LOCATION}
%test.mp.jwt.verify.issuer=reply-core

%test.mp.jwt.verify.publickey.location=${MP_JWT_VERIFY_PUBLICKEY_LOCATION}